package com.example.bugwise.dto;

public record InsectOrderDTO(Long id,
                             String name,
                             String latinName,
                             String description
                             ) {
}
